server:
  port: 8080
spring:
  datasource:
    url: jdbc:p6spy:postgresql://localhost:5433/tshop?currentSchema=public
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    username: postgres
    password: 123@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
keycloak:
  client-id: tiez_shop
  client-secret: vTaT5bzOzh36aqTcdb2uGNTuxPUcv4lV
  realm: tiez-shop
  base-url: http://localhost:8180
  registration-url: "${keycloak.base-url}/admin/realms/${keycloak.realm}/users"
  token-url: "${keycloak.base-url}/realms/${keycloak.realm}/protocol/openid-connect/token"
